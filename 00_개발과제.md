# 💼 Tax Workbench: Deep-Dive Implementation

## 🚀 [Mission] 세무 전문가들의 '전쟁터'를 구할 워크벤치를 만들어주세요

### 1. 1명이 200개를 관리하는 '극한의 3월'을 해결해야 해요

법인세 신고 기간인 3월, 세무사 한 분이 적게는 50곳에서 많게는 200곳이 넘는 고객사의 세금 신고를 동시에 처리합니다. 문제는 고객사마다 **등급(tier)**과 **유형(type)**에 따라 처리 방식이 달라진다는 거예요. VIP 고객사는 전담 관리가 필요하고, 법인과 개인사업자는 신고 절차 자체가 다르죠. 이 복잡한 규칙들이 얽혀 있는 상황에서도 데이터가 꼬이지 않고 정합성을 유지하도록 설계해야 해요.

### 2. 엑셀보다 강력한 '워크벤치(Workbench)'가 필요해요

바쁜 현장에서 매번 페이지를 새로고침하며 데이터를 수정하는 건 비효율적이에요. 수천 건의 데이터를 한 화면에서 훑어보고, 클릭 없이 **키보드만으로** 원하는 셀을 찾아 즉시 수정할 수 있어야 합니다. 물론, 여러 담당자가 같은 데이터를 동시에 건드려도 문제없이 동작해야 하고요.

### 3. 데이터의 신뢰와 성능을 책임져 주세요

3년만 운영해도 데이터는 **10만 건**을 넘어갑니다. 스크롤 할 때마다 버벅거리거나, Export 버튼에 서버가 뻗으면 안 돼요. 대량 등록(Bulk Insert)이든 수만 건 내보내기(Export)든, 시스템은 **안정적으로 스트리밍** 되어야 합니다. 또한 "어제 이 필드 뭐였지?"라는 질문에 즉시 답할 수 있도록, **필드 단위 변경 이력**을 수백만 건 쌓아도 조회 성능에 영향이 없어야 해요. 단순히 기능이 돌아가는 것을 넘어, 지속 가능한 아키텍처와 사용자 경험을 깊이 있게 고민하여 프로덕션 레벨에서도 통할 수 있는 결과물을 보여주세요.

> 우리는 정석적인 구현을 넘어, **본인의 기술적 철학과 깊이가 투영된 결과물**을 기대하며, 이유 있는 오버엔지니어링(Over-engineering)을 환영합니다.

## 🤖 AI Collaboration Policy

- 본 과제는 AI(ChatGPT, Claude, Copilot 등)의 사용을 **권장**합니다.
- 단, AI가 생성한 보편적이고 평이한 코드(Standard Layered CRUD)를 그대로 제출하는 것은 지양해 주십시오.
- **AI의 제안을 비판적으로 수용**하고, 프로젝트의 아키텍처적 완성도를 높이기 위해 AI를 어떻게 '리드'했는지가 중요한 평가 요소입니다.

## 🛠 Technology Stack

- **Backend**: Java 21, Spring Boot 4.x, Database 자유 (H2 권장)
- **Frontend**: React 19, TypeScript, Tailwind CSS

## 📋 Minimum Data Model (Entity)

### WorkItem

- `id`: PK
- `clientName`: Client's name (Required)
- `bizNo`: Business registration number
- `type`: Work type (FILING, BOOKKEEPING, REVIEW, ETC)
- `status`: Work status (TODO, IN_PROGRESS, DONE, HOLD)
- `assignee`: Persona in charge
- `dueDate`: Deadline
- `tags`: Array of strings
- `memo`: Additional notes
- `updatedAt`: Last modification timestamp

### Client (The Entity Managing Work)

- `id`: PK
- `name`: Client's legal name
- `bizNo`: Business registration number / Tax ID
- `type`: INDIVIDUAL, CORPORATE
- `status`: ACTIVE, INACTIVE
- `tier`: BASIC, PREMIUM, VIP (HINT: May affect business logic)

> [!TIP]
> 위 필드 리스트는 최소한의 가이드입니다. 본인의 설계 의도와 기능 요구사항(정합성, 성능 등)에 맞춰 필드를 자유롭게 추가하거나 엔티티 스키마를 고도화하십시오.

---

## 📋 최소 기능 요구사항 (Minimum Functional Requirements)

아래 기능은 워크벤치의 기본 동작을 위해 **반드시 구현**되어야 하는 최소 범위입니다. 이 기반 위에서 뒤에 나오는 'Challenges'를 해결하십시오.

1.  **워크벤치 대시보드 (Listing)**
    - `WorkItem` 목록 조회 및 페이징.
    - 업체명(`Client.name`), 상태, 담당자, 마감일 기준 필터링 및 정렬.
2.  **실시간 업무 수정 (Management)**
    - 목록에서 직접 업무 상태나 마감일을 수정하는 인라인 편집(Inline Edit) 기능.
    - 새로운 업무(`WorkItem`) 생성 및 특정 업체(`Client`) 할당.
3.  **데이터 대량 연동 (Integrations)**
    - API를 통한 수천 건 이상의 `WorkItem` 대량 등록(Bulk Insert) 기능.
4.  **정보 내보내기 (Export)**
    - 현재 필터링된 목록 전체를 CSV 파일로 다운로드.
5.  **변경 이력 조회 (Auditing UI)**
    - 특정 업무의 상세 변경 이력(필드별 이전/이후 값)을 확인할 수 있는 UI.

---

## 📌 핵심 비즈니스 요구사항 (Challenges)

### 1. 설계의 지속 가능성 (Sustainability)

- **Challenge**: 특정 기술 스택(프레임워크, 라이브러리)의 변화에 비즈니스 로직이 흔들리지 않도록 유연하게 설계하십시오. 코드의 결합도와 응집도에 대한 본인만의 철학을 보여주십시오.

### 2. 정합성과 동시성 극치 (Consistency)

- **Challenge**: 여러 사용자가 수만 개의 항목을 동시에 편집할 때 발생할 수 있는 모든 레이스 컨디션을 완벽히 방어하십시오. 또한 충돌 발생 시 사용자가 이를 합리적으로 해결(예: 시각적 비교/머지)할 수 있는 최상급 UX를 고민하십시오.

### 3. 고성능 대용량 데이터 처리 (Scalability)

- **Challenge**: 10만 건 이상의 데이터셋에서도 사용자 경험을 저해하지 않는 수준의 고성능 페이징 및 필터 로직을 설계하십시오. 대량 등록(Bulk Insert) 및 대량 내보내기(Export) 시나리오에서도 시스템이 안정적으로 동작해야 합니다.

### 4. 무결한 변경 이력 관리 (Auditing)

- **Challenge**: 필드 단위의 상세한 변경 이력을 기록하되, 이 데이터가 수백만 건이 쌓여도 목록 조회 및 저장 성능에 0.1ms의 지연도 주지 않는 구조를 고민하십시오.

### 5. 대량 데이터 스트리밍 서비스 (Streaming)

- **Challenge**: **현재 필터링된** 수만 건의 데이터를 Export 할 때에도 시스템의 안정성을 유지하며 대용량 데이터를 효율적으로 전달할 수 있는 안전한 스트리밍 파이프라인을 구축하십시오.

### 6. 복합 도메인 비즈니스 규칙 (Complex Domain Business Rules)

- **Challenge**: `WorkItem`은 특정 `Client`에 귀속되며, 해당 `Client`의 속성(`tier`, `type` 등)에 따라 `WorkItem`의 생성 및 관리 로직이 유기적으로 변화해야 합니다.
- 두 도메인 간의 상호작용이 비즈니스 요구사항에 따라 데이터 정합성을 유지하며 동작하도록 설계하십시오. 특히 한 도메인의 변화가 다른 도메인에 미치는 파급 효과를 무결하게 처리하는 본인만의 설계 해법을 제시하십시오.

### 7. 프론트엔드 아키텍처 및 사용자 경험 (Frontend Architecture & UX)

- **Challenge**: 단순히 동작하는 UI를 넘어, **지속 가능한 컴포넌트 아키텍처**와 **프로덕션급 안정성**을 갖춘 프론트엔드를 구축하십시오. 10만 건의 데이터를 처리하는 대시보드에서도 사용자 경험을 저해하지 않는 성능과, 누구나 접근 가능한 포용적 설계를 고민하십시오.

---

## 📦 제출 결과물 (Deliverables)

1. **소스 코드**: 설계 의도가 명확히 드러나는 정교한 아키텍처 기반의 백엔드 및 프론트엔드 코드.
2. **README.md (The Architect's Log)**:
   - **How to Run**: 프로젝트 빌드 및 실행 방법 (의존성 설치, 환경 변수 설정 등 포함).
   - **Architectural Rationale**: 왜 이 구조를 선택했는가? (지향하는 기술적 원칙 및 구조적 결정 근거 기술)
   - **Trade-off Analysis**: 선택한 설계가 가져온 복잡도와 유지보수성 사이의 타협점.
   - **Cloud & PaaS Strategy**: 실제 프로덕션 환경에서 트래픽과 데이터가 급증하는 상황을 가정하여, 클라우드 PaaS(예: Azure Functions, Service Bus, Container Apps 등)를 기반으로 시스템을 어떻게 전략적으로 확장하고 고도화할 것인지에 대한 아키텍처 설계 전략.
   - **AI Collaboration Reflection**: AI를 어떻게 활용했고, AI의 한계를 어떻게 극복했는가?
3. **API 명세 (Optional)**: (Markdown 혹은 자유 형식).
